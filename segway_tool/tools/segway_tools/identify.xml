<tool id="segway_tool_identify" name="Annotate" version="0.0.1">
  <description></description>
  <command>
  <![CDATA[segway identify --include-coords="$include_coords" --exclude-coords="$exclude_coords" --output_label="$output_label" --seg-table="$seg_table" --virtual-evidence="$virtual_evidence" "$genodata" "./traindir" "./annotatedir" > $output
]]>
  </command>

  <inputs>
    <param name="genodata" type="data" label="Genomedata"/>
    <param name="include_coords" type="data" label="Include Coords" help="Limit to genomic coordinates in FILE (default all)"/>
    <param name="exclude_coords" type="data" label="Exclude Coords" help="Filter out genomic coordinates in FILE (default none)"/>
    <param name="output_label" type="select" label="Output Label" help="In the segmentation file, select which labels to print">
	<option value="seg" selected="True">Superlabel only</option>
	<option value="subseg">Sublabel only</option>
	<option value="full">Superlabel and sublabel</option>
    </param>
    <param name="seg_table" type="data" label="Segment hyperparameters" help="Load segment hyperparameters from FILE (default none)"/>
    <param name="virtual_evidence" type="data" label="Virtual Evidence" help="virtual evidence with priors for labelsat each position in FILE (default none)"/>
  </inputs>
  <outputs>
    <data auto_format="true" name="output" />
  </outputs>

  <tests>
    <!--<test>
      <param name="input" value="fa_gc_content_input.fa"/>
      <output name="out_file1" file="fa_gc_content_output.txt"/>
    </test>-->
  </tests>

  <help>
  </help>

</tool>
