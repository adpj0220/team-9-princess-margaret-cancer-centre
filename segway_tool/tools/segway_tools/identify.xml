<tool id="segway_tool_identify" name="Annotate" version="0.0.1">
  <description></description>
  <command>segway identify --include-coords="$include_coords" --exclude-coords="$exclude_coords" --output_label="$output_label" "$genodata" "./traindir" "./annotatedir" > $output</command>
  <inputs>
    <param name="genodata" type="data" label="Genomedata"/>
    <param name="include_coords" type="data" label="Include Coords" help="Limit to genomic coordinates in FILE (default all)"/>
    <param name="exclude_coords" type="data" label="Exclude Coords" help="Filter out genomic coordinates in FILE (default none)"/>
    <param name="output_label" type="select" label="Output Label" help="">
	<option value="full" selected=True>Print superlabel and sublabel</option>
	<option value="seg">Print only superlabel</option>
	<option value="subseg">Print only sublabel</option>
    </param>
  </inputs>
  <outputs>
    <data auto_format="true" name="output" />
  </outputs>

  <tests>
    <!--<test>
      <param name="input" value="fa_gc_content_input.fa"/>
      <output name="out_file1" file="fa_gc_content_output.txt"/>
    </test>-->
  </tests>

  <help>
  </help>

</tool>
