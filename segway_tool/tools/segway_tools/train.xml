<tool id="segway_tool_train" name="Train model" version="0.0.1">
  <description></description>
  <command>
  <![CDATA[
segway '--cluster-opt=' train --include-coords='$include_coords' --exclude-coords="$exclude_coords" --tracks-from='$tracks' --max-train-rounds='$max_train_rounds' --num-instances='$num_instances' --num-labels='$num_labels' --resolution='$resolution' '$genodata' './traindir' 2> $output && find */segway -type d -name "traindir" -exec zip -r {}.zip {} ';'
]]>
  </command>



  <inputs>
    <param name="genodata" type="data" label="Genomedata"/>
    <param name="include_coords" type="data" label="Include Coords" help="Limit to genomic coordinates in FILE (default all)"/>
    <param name="exclude_coords" type="file" label="Exclude Coords" help="Filter out genomic coordinates in FILE (default none)"/>
    <param name="tracks" type="data" label="Tracks" help="Append tracks from newline-delimited FILE to list of tracks to use"/>
    <param name="max_train_rounds" type="integer" value="100" min="1" label="Max Train Rounds" help="Each training instance runs a maximum of NUM rounds"/>
    <param name="num_instances" type="integer" value="1" min="1" label="Number of Instances" help="Run NUM training instances, randomizing start parameters NUM
times"/>
    <param name="num_labels" type="integer" value="2" min="1" label="Number of labels" help="Make SLICE segment labels"/>
    <param name="resolution" type="integer" value="1" label="Resolution" help="Downsample to every RES bp"/>
  </inputs>
  <outputs>
    <data auto_format="true" name="output" />
  </outputs>

  <tests>
    <!--<test>
      <param name="input" value="fa_gc_content_input.fa"/>
      <output name="out_file1" file="fa_gc_content_output.txt"/>
    </test>-->
  </tests>

  <help>
  </help>

</tool>
